import { StyleSheet } from 'react-native'

export const ScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 20,
  },
  buttonText: {
    textAlign: 'center',
    fontWeight: 'semibold',
    fontSize: 18,
    paddingBottom: 10,
    color: 'black',
  },
})
